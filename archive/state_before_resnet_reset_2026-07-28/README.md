# State snapshot before the ResNet V9 reset

This directory preserves the full current-state, handoff, README, machine manifest, old active algorithm contract, and tooling snapshot immediately before the repository was simplified around the ResNet-18-GN candidate.

Nothing here is an active execution contract. Use the files at their normal repository paths for current truth.
