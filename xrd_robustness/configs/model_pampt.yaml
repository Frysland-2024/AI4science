backbone: pampt
variant: b3
input_length: 3501
patch_size: 16
patch_stride: 8
embed_dim: 128
depth: 4
num_heads: 4
mlp_ratio: 4.0
dropout: 0.1
pooling: mean
num_classes: 7
local_kernels: [5, 11, 21]
branch_channels: 32
fusion_dim: 128

# Derivatives use fixed finite differences and reflect padding.
# B0 uses non-overlapping patches; B1-B3 use patch_stride=8.
